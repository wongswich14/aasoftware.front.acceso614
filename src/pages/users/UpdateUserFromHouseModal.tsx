interface UpdateUserFromHouseModalProps {
    toggleModal: () => void
}

const UpdateUserFromHouseModal: React.FC<UpdateUserFromHouseModalProps> = ({ toggleModal }) => {
    return (
        <div>
        <h1>Update User From House Modal</h1>
        </div>
    );
}

export default UpdateUserFromHouseModal;